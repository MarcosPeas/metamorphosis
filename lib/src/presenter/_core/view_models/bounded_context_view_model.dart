/*import 'package:metamorphis/src/domain/bounded_context/entities/bounded_context.dart';

class BoundedContextViewModel extends BoundedContext {
  BoundedContextViewModel({
    super.id,
    required super.name,
    required super.enabled,
    required super.applicationId,
    super.createdAt,
  });

  factory BoundedContextViewModel.fromEntity(BoundedContext boundedContext) {
    return BoundedContextViewModel(
      id: boundedContext.id,
      name: boundedContext.name,
      enabled: boundedContext.enabled,
      applicationId: boundedContext.applicationId,
      createdAt: boundedContext.createdAt,
    );
  }
}*/
