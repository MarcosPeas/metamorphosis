name: metamorphis
description: "Projeto He Coder"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.2
  cloud_firestore: ^5.1.0
  go_router: ^14.2.0
  get_it: ^7.7.0
  dartz: ^0.10.1
  localization: ^2.1.1
  uuid: ^4.5.1
  intl: ^0.20.2
  archive: ^4.0.7
  change_case: ^2.1.0
  supabase_flutter: ^2.8.0
  flashy_flushbar: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/i18n/